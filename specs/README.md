# specs/

One folder per feature: `specs/<3-digit-id>-<kebab-slug>/`.

Each folder holds `spec.md`, `plan.md`, `tasks.md`,
`checklists/requirements.md` (see `.specify/templates/`).

Baseline: `001-baseline-tasks-mcp-server/` documents the current server.
